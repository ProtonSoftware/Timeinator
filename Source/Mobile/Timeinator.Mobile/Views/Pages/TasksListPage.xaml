<?xml version="1.0" encoding="utf-8" ?>
<local:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Timeinator.Mobile"
             x:Class="Timeinator.Mobile.TasksListPage"
             Title="{Binding Title}">

    <local:BasePage.ToolbarItems>
        <ToolbarItem Name="+" Command="{Binding AddNewTaskCommand}" />
    </local:BasePage.ToolbarItems>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <ScrollView>

            <local:ItemsControl ItemsSource="{Binding TaskItems}">

                <local:ItemsControl.PlaceholderText>
                    Brak zadań na dzisiaj - aby je dodać, kliknij ikonkę + u góry
                </local:ItemsControl.PlaceholderText>

                <local:ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:TimeTaskControl />
                    </DataTemplate>
                </local:ItemsControl.ItemTemplate>

            </local:ItemsControl>

        </ScrollView>

        <StackLayout Grid.Row="1">

            <TimePicker Time="{Binding UserTime}" />
            
            <Button Text="GOTOW"
                Command="{Binding UserReadyCommand}"
                Grid.Row="1" />

        </StackLayout>

    </Grid>

</local:BasePage>
