<?xml version="1.0" encoding="utf-8" ?>
<local:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Timeinator.Mobile"
             x:Class="Timeinator.Mobile.TasksPreparationPage"
             Title="{Binding Title}">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <ScrollView>

            <local:ItemsControl ItemsSource="{Binding TaskItems}">

                <local:ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:CalculatedTimeTaskControl />
                    </DataTemplate>
                </local:ItemsControl.ItemTemplate>

            </local:ItemsControl>

        </ScrollView>

        <Button Text="START ZADAN"
                Command="{Binding StartTasksCommand}"
                Grid.Row="1" />

    </Grid>

</local:BasePage>
