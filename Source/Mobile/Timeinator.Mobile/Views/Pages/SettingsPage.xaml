<?xml version="1.0" encoding="utf-8" ?>
<local:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Timeinator.Mobile"
             x:Class="Timeinator.Mobile.SettingsPage"
             Title="{Binding Title}">

    <TableView Intent="Settings" Margin="10">
        <TableRoot>
            <TableSection Title="Zadania">
                <SwitchCell Text="Zadania z najwyższym priorytetem jako pierwsze"
                            On="{Binding HighestPrioritySetAsFirst}"/>
            </TableSection>
            <TableSection Title="Inne">
                <SwitchCell Text="Zmień jezyk" />
                <SwitchCell Text="Ciemny motyw"
                            On="{Binding IsDarkModeOn}" />
            </TableSection>
        </TableRoot>
    </TableView>


</local:BasePage>
